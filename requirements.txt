# Agent-based SIR spatial simulation — use Python 3.10 or newer.
# Install: python -m pip install -r requirements.txt

numpy>=1.24.0
matplotlib>=3.7.0

# Optional: saving animations with Simulation.save_live_animation(..., writer="pillow").
pillow>=10.0.0
